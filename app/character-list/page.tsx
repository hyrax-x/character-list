const CharacterList = () => {
  return (
    <main className="page" id="character-list">
      <section className={`section`}>
        <div className={`sectionContent`}>
          <h1>Character list</h1>
        </div>
      </section>
    </main>
  );
};

export default CharacterList;
